[Unit]
Description=Photon Dashboard
After=graphical.target

[Service]
Type=simple
User=root
Environment=DISPLAY=:0
ExecStartPre=/usr/bin/startx /root/.xinitrc -- :0 vt1 &
ExecStart=/usr/bin/startx /root/.xinitrc -- :0 vt1
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical.target
